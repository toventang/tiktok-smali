package com.google.gson;

import com.bytedance.covode.number.Covode;
import java.lang.reflect.Type;

public interface j {
    static {
        Covode.recordClassIndex(34055);
    }

    <T> T a(l lVar, Type type);
}
