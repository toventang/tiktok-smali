package com.squareup.a;

import com.bytedance.covode.number.Covode;
import java.net.Proxy;

public interface b {
    static {
        Covode.recordClassIndex(35986);
    }

    v a(Proxy proxy, x xVar);

    v b(Proxy proxy, x xVar);
}
