package com.apiguard3.internal;

import com.bytedance.covode.number.Covode;

public interface de {
    static {
        Covode.recordClassIndex(2540);
    }

    byte[] AGRequest(byte[] bArr);

    byte[] getHeaders(byte[] bArr);
}
