package com.bytedance.sdk.bridge.a;

import com.bytedance.covode.number.Covode;

public @interface d {
    static {
        Covode.recordClassIndex(26804);
    }

    String a();

    int b() default 0;

    long c() default 0;

    double d() default 0.0d;

    float e() default 0.0f;

    boolean f() default false;

    String g() default "";

    boolean h() default false;
}
