package f.a;

import com.bytedance.covode.number.Covode;

public interface y<Upstream, Downstream> {
    static {
        Covode.recordClassIndex(104946);
    }

    x<Downstream> a(t<Upstream> tVar);
}
