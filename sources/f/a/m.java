package f.a;

import com.bytedance.covode.number.Covode;
import org.a.b;

public interface m<Upstream, Downstream> {
    static {
        Covode.recordClassIndex(104931);
    }

    b<Downstream> a(h<Upstream> hVar);
}
