package kotlinx.coroutines.flow;

import com.bytedance.covode.number.Covode;
import h.c.d;
import h.z;

public interface a<T> {
    static {
        Covode.recordClassIndex(105629);
    }

    Object a(T t, d<? super z> dVar);
}
