package kotlinx.coroutines;

import com.bytedance.covode.number.Covode;
import h.c.f;

public interface cv<S> extends f.b {
    static {
        Covode.recordClassIndex(105613);
    }

    S a(f fVar);

    void a(S s);
}
