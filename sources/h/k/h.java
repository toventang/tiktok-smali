package h.k;

import com.bytedance.covode.number.Covode;
import h.f.a.m;
import h.k.f;
import h.z;

public interface h<T, V> extends f<V>, k<T, V> {

    public interface a<T, V> extends m<T, V, z>, f.a<V> {
        static {
            Covode.recordClassIndex(105289);
        }
    }

    static {
        Covode.recordClassIndex(105288);
    }

    a<T, V> getSetter();
}
