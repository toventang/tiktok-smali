package h.k;

import com.bytedance.covode.number.Covode;
import h.z;

public interface f<V> extends i<V> {

    public interface a<V> extends e<z> {
        static {
            Covode.recordClassIndex(105285);
        }
    }

    static {
        Covode.recordClassIndex(105284);
    }
}
