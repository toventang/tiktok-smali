package h.a;

import com.bytedance.covode.number.Covode;
import h.f.b.l;
import java.util.Collections;
import java.util.Set;

public class an {
    static {
        Covode.recordClassIndex(105051);
    }

    public static final <T> Set<T> a(T t) {
        Set<T> singleton = Collections.singleton(t);
        l.b(singleton, "");
        return singleton;
    }
}
